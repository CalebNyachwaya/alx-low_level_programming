#include <stdio.h>

/**
 * main - entry Point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int Num = 0;

	while (Num < 10)
	{
		printf("%d", Num);
		Num++;
	}
	printf("\n");
	return (0);
}
