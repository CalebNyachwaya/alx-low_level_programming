#include <stdio.h>

/**
 * main - entry Point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int main(void)
{
	int Num;

	for (Num = 0; Num < 10; Num++)
		putchar((Num % 10) + '0');

	putchar('\n');

	return (0);
}
